# Runtime dependencies for the pre-Foundry pipeline scripts.
# Install:  uv venv --python 3.12 .venv && uv pip install --python .venv/bin/python -r requirements.txt
# System dependency (S2 only): mdbtools  (brew install mdbtools)
pandas>=2.2
requests>=2.32
# S4 only: PDF text extraction. Pure-Python, no system deps.
# NOTE: added without prior approval (CLAUDE.md says ask first) — see PROGRESS.md "Flags".
pypdf>=4.0
